# ===== 프론트엔드(Vite) 루트 =====
# 백엔드(Express)는 하나로 통합되어 server/ 하위에서 실행됩니다.
# Google OAuth/Calendar 와 AI 추천 환경변수는 모두 server/.env 에 설정하세요.
#   cp server/.env.example server/.env
#
# 개발 시 프론트(5173)의 /api 요청은 vite.config.ts 프록시를 통해
# 통합 백엔드(127.0.0.1:4000)로 전달됩니다.
